-- [[ ARCHITECT V7.0: OBSIDIAN BRUTALISM ]] --
-- [[ HIGH-PERFORMANCE / PRO BUILDER / NO-LAG ]] --

local CoreGui = game:GetService("CoreGui")
local TweenService = game:GetService("TweenService")
local RunService = game:GetService("RunService")
local UserInputService = game:GetService("UserInputService")
local Players = game:GetService("Players")
local HttpService = game:GetService("HttpService")
local TeleportService = game:GetService("TeleportService")
local LocalPlayer = Players.LocalPlayer
local Camera = workspace.CurrentCamera

-- [ ПЕРСИСТЕНТНОСТЬ ]
if queue_on_teleport then queue_on_teleport('loadstring(game:HttpGet("https://raw.githubusercontent.com/script/main.lua"))()') end

-- [ БЛОК 1: ULTRA-SHIELD ANTI-DETECT ]
local function Protect()
    local gmt = getrawmetatable(game)
    local old_nc = gmt.__namecall
    setreadonly(gmt, false)
    gmt.__namecall = newcclosure(function(self, ...)
        local method = getnamecallmethod()
        if not checkcaller() then
            if method == "FindFirstChild" or method == "GetChildren" then
                if self == CoreGui or self == LocalPlayer.PlayerGui then return nil end
            end
        end
        return old_nc(self, ...)
    end)
    setreadonly(gmt, true)
end
Protect()

-- [ БЛОК 2: BRUTAL UI ENGINE ]
local Screen = Instance.new("ScreenGui", CoreGui)
Screen.Name = "Obsidian_Core"
Screen.ResetOnSpawn = false

local Theme = {
    Main = Color3.fromRGB(5, 5, 5),
    Panel = Color3.fromRGB(15, 15, 15),
    Accent = Color3.fromRGB(0, 180, 255),
    Text = Color3.fromRGB(255, 255, 255),
    Font = Enum.Font.RobotoMono
}

local MainFrame = Instance.new("Frame", Screen)
MainFrame.Size = UDim2.new(0, 360, 0, 400)
MainFrame.Position = UDim2.new(0.5, -180, 0.5, -200)
MainFrame.BackgroundColor3 = Theme.Main
MainFrame.BorderSizePixel = 0
local Stroke = Instance.new("UIStroke", MainFrame); Stroke.Color = Theme.Accent; Stroke.Thickness = 1

local TopBar = Instance.new("Frame", MainFrame)
TopBar.Size = UDim2.new(1, 0, 0, 35)
TopBar.BackgroundColor3 = Theme.Panel
TopBar.BorderSizePixel = 0

local Title = Instance.new("TextLabel", TopBar)
Title.Size = UDim2.new(1, -40, 1, 0); Title.Position = UDim2.new(0, 15, 0, 0)
Title.Text = "ARCHITECT V7.0 // OBSIDIAN"; Title.TextColor3 = Theme.Text; Title.Font = Theme.Font; Title.TextSize = 14; Title.TextXAlignment = "Left"; Title.BackgroundTransparency = 1

local ToggleBtn = Instance.new("TextButton", TopBar)
ToggleBtn.Size = UDim2.new(0, 30, 0, 30); ToggleBtn.Position = UDim2.new(1, -35, 0, 2.5)
ToggleBtn.Text = "_"; ToggleBtn.TextColor3 = Theme.Accent; ToggleBtn.BackgroundColor3 = Theme.Main; ToggleBtn.BorderSizePixel = 0

local Scroll = Instance.new("ScrollingFrame", MainFrame)
Scroll.Size = UDim2.new(1, -10, 1, -45); Scroll.Position = UDim2.new(0, 5, 0, 40)
Scroll.BackgroundTransparency = 1; Scroll.BorderSizePixel = 0; Scroll.ScrollBarThickness = 2; Scroll.ScrollBarImageColor3 = Theme.Accent
Instance.new("UIListLayout", Scroll).Padding = UDim.new(0, 4)

local RestoreBtn = Instance.new("TextButton", Screen)
RestoreBtn.Size = UDim2.new(0, 45, 0, 45); RestoreBtn.Position = UDim2.new(0, 20, 0.5, -22); RestoreBtn.Visible = false
RestoreBtn.BackgroundColor3 = Theme.Main; RestoreBtn.Text = "A"; RestoreBtn.TextColor3 = Theme.Accent; RestoreBtn.Font = Theme.Font; RestoreBtn.TextSize = 20
Instance.new("UIStroke", RestoreBtn).Color = Theme.Accent

-- [ BUILD HUB SUB-MENU ]
local BuildPanel = Instance.new("Frame", Screen)
BuildPanel.Size = UDim2.new(0, 220, 0, 320)
BuildPanel.Position = UDim2.new(0.5, 190, 0.5, -160)
BuildPanel.BackgroundColor3 = Theme.Panel; BuildPanel.Visible = false
Instance.new("UIStroke", BuildPanel).Color = Theme.Accent

local function AddBuildControl(name, options, callback)
    local l = Instance.new("TextLabel", BuildPanel)
    l.Size = UDim2.new(1, 0, 0, 20); l.Text = name:upper(); l.TextColor3 = Theme.Accent; l.Font = Theme.Font; l.TextSize = 10; l.BackgroundTransparency = 1
    
    local combo = Instance.new("TextBox", BuildPanel)
    combo.Size = UDim2.new(1, -20, 0, 30); combo.BackgroundColor3 = Theme.Main; combo.Text = options[1]; combo.TextColor3 = Theme.Text; combo.Font = Theme.Font; combo.TextSize = 12
    combo.FocusLost:Connect(function() callback(combo.Text) end)
    
    local list = Instance.new("UIListLayout", BuildPanel); list.Padding = UDim.new(0, 5)
end

-- [ ФУНКЦИИ UI ]
local function MoveUI(frame)
    local dragStart, startPos
    frame.InputBegan:Connect(function(i) if i.UserInputType == Enum.UserInputType.MouseButton1 or i.UserInputType == Enum.UserInputType.Touch then dragStart = i.Position; startPos = frame.Position end end)
    UserInputService.InputChanged:Connect(function(i) if dragStart and (i.UserInputType == Enum.UserInputType.MouseMovement or i.UserInputType == Enum.UserInputType.Touch) then
        local delta = i.Position - dragStart
        frame.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y)
    end end)
    UserInputService.InputEnded:Connect(function(i) if i.UserInputType == Enum.UserInputType.MouseButton1 or i.UserInputType == Enum.UserInputType.Touch then dragStart = nil end end)
end
MoveUI(MainFrame)

local function ToggleUI()
    if MainFrame.Visible then
        TweenService:Create(MainFrame, TweenInfo.new(0.4, Enum.EasingStyle.Quart), {Size = UDim2.new(0, 360, 0, 0)}):Play()
        task.wait(0.4); MainFrame.Visible = false; RestoreBtn.Visible = true; BuildPanel.Visible = false
    else
        RestoreBtn.Visible = false; MainFrame.Visible = true
        TweenService:Create(MainFrame, TweenInfo.new(0.4, Enum.EasingStyle.Quart), {Size = UDim2.new(0, 360, 0, 400)}):Play()
    end
end
ToggleBtn.MouseButton1Click:Connect(ToggleUI); RestoreBtn.MouseButton1Click:Connect(ToggleUI)

local function CreateToggle(name, callback)
    local s = false
    local b = Instance.new("TextButton", Scroll)
    b.Size = UDim2.new(1, -10, 0, 35); b.BackgroundColor3 = Theme.Panel; b.Text = name:upper().." [OFF]"; b.TextColor3 = Theme.Text; b.Font = Theme.Font; b.TextSize = 12; b.BorderSizePixel = 0
    b.MouseButton1Click:Connect(function()
        s = not s; b.Text = name:upper()..(s and " [ON]" or " [OFF]"); b.TextColor3 = s and Theme.Accent or Theme.Text
        callback(s)
    end)
end

-- [ ОБНОВЛЕННЫЕ ФУНКЦИИ ]

-- 1. NO-LAG PRECISION FLY
local flyActive, flySpeed = false, 60
local bv, bg
CreateToggle("Ghost Fly", function(s)
    flyActive = s
    local char = LocalPlayer.Character
    if s and char then
        char.Humanoid.PlatformStand = true
        bv = Instance.new("BodyVelocity", char.HumanoidRootPart); bv.MaxForce = Vector3.new(1e6, 1e6, 1e6)
        bg = Instance.new("BodyGyro", char.HumanoidRootPart); bg.MaxTorque = Vector3.new(1e6, 1e6, 1e6)
        
        local flyConn
        flyConn = RunService.Heartbeat:Connect(function()
            if not flyActive then flyConn:Disconnect(); return end
            local dir = Vector3.new(0,0,0)
            if UserInputService:IsKeyDown(Enum.KeyCode.W) then dir = dir + Camera.CFrame.LookVector end
            if UserInputService:IsKeyDown(Enum.KeyCode.S) then dir = dir - Camera.CFrame.LookVector end
            if UserInputService:IsKeyDown(Enum.KeyCode.A) then dir = dir - Camera.CFrame.RightVector end
            if UserInputService:IsKeyDown(Enum.KeyCode.D) then dir = dir + Camera.CFrame.RightVector end
            
            bv.Velocity = dir.Magnitude > 0 and dir.Unit * flySpeed or Vector3.new(0, 0.1, 0)
            bg.CFrame = Camera.CFrame
            for _, v in pairs(char:GetDescendants()) do if v:IsA("BasePart") then v.CanCollide = false end end
        end)
    else
        if bv then bv:Destroy(); bg:Destroy() end
        if char then char.Humanoid.PlatformStand = false
            for _, v in pairs(char:GetDescendants()) do if v:IsA("BasePart") then v.CanCollide = true end end
        end
    end
end)

-- 2. SUPREME INVISIBLE (CLEAN)
CreateToggle("Supreme Invisible", function(s)
    local char = LocalPlayer.Character
    if char then
        for _, v in pairs(char:GetDescendants()) do
            if v:IsA("BasePart") or v:IsA("Decal") then v.Transparency = s and 1 or 0 end
            if v:IsA("BillboardGui") or v:IsA("TextLabel") then v.Enabled = not s end
        end
    end
end)

-- 3. BRUTAL BUILD HUB
local buildMode = false
local b_shape, b_mat, b_color = "Block", "Neon", Theme.Accent
CreateToggle("Build Hub Menu", function(s) BuildPanel.Visible = s; buildMode = s end)

AddBuildControl("Shape", {"Block", "Ball", "Cylinder"}, function(v) b_shape = v end)
AddBuildControl("Material", {"Neon", "Glass", "Plastic", "Metal"}, function(v) b_mat = v end)

UserInputService.InputBegan:Connect(function(i, g)
    if buildMode and not g and (i.UserInputType == Enum.UserInputType.MouseButton1 or i.UserInputType == Enum.UserInputType.Touch) then
        local ray = Camera:ScreenPointToRay(UserInputService:GetMouseLocation().X, UserInputService:GetMouseLocation().Y)
        local hit = workspace:Raycast(ray.Origin, ray.Direction * 1000)
        if hit then
            local p = Instance.new("Part", workspace)
            p.Shape = Enum.PartType[b_shape]; p.Material = Enum.Material[b_mat]
            p.Color = b_color; p.Size = Vector3.new(4, 4, 4); p.Anchored = true; p.Position = hit.Position + Vector3.new(0, 2, 0)
        end
    end
end)

-- [ БЛОК 4: ОРИГИНАЛЬНЫЙ КОД (НЕ ТРОНУТО) ]
CreateToggle("Infinite Jump", function(s)
    local conn = UserInputService.JumpRequest:Connect(function() if s then LocalPlayer.Character.Humanoid:ChangeState(3) end end)
end)

CreateToggle("Architect ESP", function(s)
    RunService.RenderStepped:Connect(function()
        if not s then return end
        for _, p in pairs(Players:GetPlayers()) do
            if p ~= LocalPlayer and p.Character and not p.Character:FindFirstChild("ArchESP") then
                Instance.new("Highlight", p.Character).Name = "ArchESP"
            end
        end
    end)
end)

local hbActive = false
CreateToggle("Hitbox Reality", function(s) hbActive = s end)
RunService.Heartbeat:Connect(function()
    for _, v in pairs(Players:GetPlayers()) do
        if v ~= LocalPlayer and v.Character and v.Character:FindFirstChild("HumanoidRootPart") then
            v.Character.HumanoidRootPart.Size = hbActive and Vector3.new(12, 12, 12) or Vector3.new(2, 2, 1)
            v.Character.HumanoidRootPart.Transparency = hbActive and 0.6 or 1
        end
    end
end)

local auraActive = false
CreateToggle("Combat Aura", function(s) auraActive = s end)
task.spawn(function()
    while task.wait(0.1) do
        if auraActive then
            for _, v in pairs(Players:GetPlayers()) do
                if v ~= LocalPlayer and v.Character and (v.Character.HumanoidRootPart.Position - LocalPlayer.Character.HumanoidRootPart.Position).Magnitude < 20 then
                    local t = LocalPlayer.Character:FindFirstChildOfClass("Tool")
                    if t then t:Activate() end
                end
            end
        end
    end
end)

print("ARCHITECT V7.0: OBSIDIAN BRUTALISM LOADED")
